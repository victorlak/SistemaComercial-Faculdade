import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    marginVertical: 24,
  },
  title: {
    fontFamily: 'Poppins-Bold',
    fontSize: 18,
    color: '#1C1C1E',
    marginBottom: 16,
  },
});